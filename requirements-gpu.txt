# ReVoice GPU-Accelerated Requirements
# Use this for NVIDIA GPU acceleration (CUDA)

-r requirements.txt

# CUDA runtime and cuDNN for faster inference
nvidia-cuda-runtime-cu12
nvidia-cudnn-cu12

# Note: You may need to install the specific version of torch for your CUDA version
# Example: pip install torch --index-url https://download.pytorch.org/whl/cu121
